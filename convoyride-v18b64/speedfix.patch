--- a/app/src/main/java/ch/piiwii/convoyride/MainActivity.java
+++ b/app/src/main/java/ch/piiwii/convoyride/MainActivity.java
@@ -32,6 +32,7 @@
     private SharedPreferences prefs;
     private LocationManager locationManager;
     private Location lastLocation;
+    private TextView speedBadge;
     private TextToSpeech tts;
     private ApiClient api;
     private boolean onlineSession=false;
@@ -82,7 +83,7 @@
     private void startBackgroundTracking(){if(!onlineSession&&!hasSavedSession())return;if(!backgroundTracking)return;prefs.edit().putBoolean("trackingEnabled",true).putBoolean("lost",lost).apply();Intent i=new Intent(this,ConvoyService.class);if(Build.VERSION.SDK_INT>=26)startForegroundService(i);else startService(i);}
     private void stopBackgroundTracking(){prefs.edit().putBoolean("trackingEnabled",false).apply();stopService(new Intent(this,ConvoyService.class));}
     private boolean trackingActive(){return prefs.getBoolean("trackingEnabled",false);}
-    private final LocationListener gpsListener=new LocationListener(){@Override public void onLocationChanged(Location l){lastLocation=l;if(screen==DRIVE)updateNavigationFromLocation(true);}};
+    private final LocationListener gpsListener=new LocationListener(){@Override public void onLocationChanged(Location l){lastLocation=l;if(screen==DRIVE){updateSpeedBadge();updateNavigationFromLocation(true);}}};
     @Override public void onRequestPermissionsResult(int r,String[] p,int[] g){super.onRequestPermissionsResult(r,p,g);if(r==77&&g.length>0&&g[0]==PackageManager.PERMISSION_GRANTED)requestGps();}
 
     private void beginPage(boolean scroll){
@@ -164,7 +165,7 @@
         screen=DRIVE;beginPage(false);page.setPadding(Ui.dp(this,10),Ui.dp(this,8),Ui.dp(this,10),Ui.dp(this,8));
         LinearLayout head=Ui.row(this);TextView brand=Ui.badge(this,"CR",Color.rgb(8,61,37),Ui.GREEN);head.addView(brand);LinearLayout htxt=Ui.col(this);TextView rn=Ui.text(this,rideName,16,Ui.TEXT,true);TextView rp=Ui.text(this,"#"+myIndex+" / "+vehicleCount+" participants",11,Ui.MUTED,false);rp.setPadding(0,Ui.dp(this,2),0,0);htxt.addView(rn);htxt.addView(rp);LinearLayout.LayoutParams hp=new LinearLayout.LayoutParams(0,-2,1);hp.setMargins(Ui.dp(this,9),0,0,0);head.addView(htxt,hp);positionBadge=Ui.badge(this,"#"+myIndex+" / "+vehicleCount,Ui.PANEL_2,Ui.GREEN);healthBadge=Ui.badge(this,"● CONVOI OK",Color.rgb(10,65,38),Ui.GREEN);head.addView(healthBadge);page.addView(head);
         page.addView(Ui.space(this,8));LinearLayout navCard=card();navCard.setPadding(Ui.dp(this,14),Ui.dp(this,13),Ui.dp(this,14),Ui.dp(this,12));instruction=Ui.text(this,onlineSession?(routeModel!=null&&routeModel.usable()?"↑  Navigation GPX prête":"↑  Suivre le trajet officiel"):"↱  Dans 300 m\nTournez à droite",24,Ui.TEXT,true);instruction.setLineSpacing(Ui.dp(this,3),1.0f);navCard.addView(instruction);routeMeta=Ui.text(this,routeModel!=null&&routeModel.usable()?routeModel.name:"Puis dans 1,2 km • tournez à gauche",11.5f,Ui.MUTED,false);routeMeta.setPadding(0,Ui.dp(this,8),0,0);navCard.addView(routeMeta);splitOverlay=Ui.text(this,"⚠  CONVOI COUPÉ — ralentissez",13,Ui.BG,true);splitOverlay.setGravity(Gravity.CENTER);splitOverlay.setPadding(Ui.dp(this,10),Ui.dp(this,7),Ui.dp(this,10),Ui.dp(this,7));splitOverlay.setBackground(Ui.bg(Ui.ORANGE,12,this));splitOverlay.setVisibility(split?View.VISIBLE:View.GONE);LinearLayout.LayoutParams sop=new LinearLayout.LayoutParams(-1,-2);sop.setMargins(0,Ui.dp(this,8),0,0);navCard.addView(splitOverlay,sop);page.addView(navCard);
-        page.addView(Ui.space(this,8));FrameLayout mapWrap=new FrameLayout(this);mapView=new ConvoyMapView(this);mapView.setMyIndex(myIndex,vehicleCount);mapView.setConvoyState(split,lost);if(routeModel!=null&&routeModel.usable())mapView.setRoute(routeModel);if(onlineSession&&snapshot!=null)mapView.setLiveParticipants(snapshot.participants);mapView.recenter();mapWrap.addView(mapView,new FrameLayout.LayoutParams(-1,-1));TextView mapBadge=Ui.badge(this,"80\nkm/h",Color.argb(225,4,9,14),Ui.TEXT);mapBadge.setTextSize(13);mapBadge.setGravity(Gravity.CENTER);FrameLayout.LayoutParams mbp=new FrameLayout.LayoutParams(Ui.dp(this,62),Ui.dp(this,62),Gravity.TOP|Gravity.LEFT);mbp.setMargins(Ui.dp(this,10),Ui.dp(this,10),0,0);mapWrap.addView(mapBadge,mbp);page.addView(mapWrap,new LinearLayout.LayoutParams(-1,0,1));
+        page.addView(Ui.space(this,8));FrameLayout mapWrap=new FrameLayout(this);mapView=new ConvoyMapView(this);mapView.setMyIndex(myIndex,vehicleCount);mapView.setConvoyState(split,lost);if(routeModel!=null&&routeModel.usable())mapView.setRoute(routeModel);if(onlineSession&&snapshot!=null)mapView.setLiveParticipants(snapshot.participants);mapView.recenter();mapWrap.addView(mapView,new FrameLayout.LayoutParams(-1,-1));speedBadge=Ui.badge(this,"GPS\n—",Color.argb(225,4,9,14),Ui.TEXT);speedBadge.setTextSize(13);speedBadge.setGravity(Gravity.CENTER);updateSpeedBadge();FrameLayout.LayoutParams mbp=new FrameLayout.LayoutParams(Ui.dp(this,62),Ui.dp(this,62),Gravity.TOP|Gravity.LEFT);mbp.setMargins(Ui.dp(this,10),Ui.dp(this,10),0,0);mapWrap.addView(speedBadge,mbp);page.addView(mapWrap,new LinearLayout.LayoutParams(-1,0,1));
         page.addView(Ui.space(this,8));LinearLayout gaps=Ui.row(this);gaps.addView(neighborCard("VOITURE DEVANT","—","—",true),new LinearLayout.LayoutParams(0,Ui.dp(this,86),1));Space sp=new Space(this);gaps.addView(sp,new LinearLayout.LayoutParams(Ui.dp(this,8),1));gaps.addView(neighborCard("VOITURE DERRIÈRE","—","—",false),new LinearLayout.LayoutParams(0,Ui.dp(this,86),1));page.addView(gaps);
         page.addView(Ui.space(this,7));LinearLayout help=Ui.row(this);TextView lostB=Ui.actionTile(this,"⚠",lost?"Retrouvé":"Perdu","",Color.rgb(111,35,39),Color.rgb(54,19,24));lostB.setTextSize(12);lostB.setOnClickListener(v->{toggleLost();if(onlineSession&&lastLocation!=null)sendPosition();showDrive();});help.addView(lostB,new LinearLayout.LayoutParams(0,Ui.dp(this,82),1));Space h1=new Space(this);help.addView(h1,new LinearLayout.LayoutParams(Ui.dp(this,6),1));TextView pause=Ui.actionTile(this,"Ⅱ","Pause","",Color.rgb(112,63,18),Color.rgb(58,34,14));pause.setTextSize(12);pause.setOnClickListener(v->sendQuickAlert("stop","Je dois m'arrêter"));help.addView(pause,new LinearLayout.LayoutParams(0,Ui.dp(this,82),1));Space h2=new Space(this);help.addView(h2,new LinearLayout.LayoutParams(Ui.dp(this,6),1));TextView fuel=Ui.actionTile(this,"⛽","Essence","",Color.rgb(110,75,15),Color.rgb(60,42,12));fuel.setTextSize(12);fuel.setOnClickListener(v->sendQuickAlert("fuel","Besoin d'essence"));help.addView(fuel,new LinearLayout.LayoutParams(0,Ui.dp(this,82),1));Space h3=new Space(this);help.addView(h3,new LinearLayout.LayoutParams(Ui.dp(this,6),1));TextView panne=Ui.actionTile(this,"🔧","Panne","",Color.rgb(109,34,46),Color.rgb(55,19,28));panne.setTextSize(12);panne.setOnClickListener(v->sendQuickAlert("breakdown","Panne signalée"));help.addView(panne,new LinearLayout.LayoutParams(0,Ui.dp(this,82),1));page.addView(help);
         page.addView(Ui.space(this,7));page.addView(bottomNav(1),new LinearLayout.LayoutParams(-1,Ui.dp(this,65)));navManeuverIndex=-999;navStage=-1;arrivalAnnounced=false;applyDriveState();updateNavigationFromLocation(false);if(onlineSession){applySnapshotToDrive();startSync();}else startTicker();
@@ -274,6 +275,8 @@
     private void resumeOnlineSession(){restoreSession();api.snapshot(rideCode,participantToken,(snap,error)->{if(snap==null){serverError(error);return;}onlineSession=true;snapshot=snap;rideName=snap.name;vehicleCount=Math.max(2,snap.expected);ensureRoute(snap);processEvents(snap);analyzeSnapshot();if("active".equalsIgnoreCase(snap.status))startBackgroundTracking();showLobby();});}
     private void serverError(String error){String msg=error==null?"Serveur ConvoyRide indisponible":error;if(msg.length()>180)msg=msg.substring(0,180);Toast.makeText(this,msg,Toast.LENGTH_LONG).show();}
     private void startSync(){if(!onlineSession)return;if(syncTicker!=null)handler.removeCallbacks(syncTicker);syncTicker=new Runnable(){@Override public void run(){if(!onlineSession)return;sendPosition();api.snapshot(rideCode,participantToken,(snap,error)->{if(snap!=null){String before=snapshotSignature(snapshot);snapshot=snap;rideName=snap.name;vehicleCount=Math.max(2,snap.expected);ensureRoute(snap);processEvents(snap);analyzeSnapshot();for(ApiClient.Participant x:snap.participants)if(x.id.equals(participantId)){myIndex=x.order;ready=x.ready;lost=x.lost;}if(screen==LOBBY){String now=snapshotSignature(snap);if(!now.equals(lastLobbySignature)){lastLobbySignature=now;if(!organizer&&"active".equals(snap.status)){startBackgroundTracking();showDrive();return;}showLobby();return;}}else if(screen==DRIVE)applySnapshotToDrive();else if(screen==ORGA){String now=snapshotSignature(snap);if(!now.equals(before)&&(!organizer||splitAfterOrder>0)){} }}handler.postDelayed(this,4000);});}};handler.post(syncTicker);}
+    private void updateSpeedBadge(){if(speedBadge==null)return;String value="GPS\n—";if(lastLocation!=null&&lastLocation.hasSpeed())value=Math.round(lastLocation.getSpeed()*3.6f)+"\nkm/h";speedBadge.setText(value);}
+
     private void sendPosition(){if(!onlineSession||lastLocation==null||participantToken.isEmpty())return;api.position(rideCode,participantToken,lastLocation.getLatitude(),lastLocation.getLongitude(),lastLocation.hasSpeed()?lastLocation.getSpeed():0,lastLocation.hasBearing()?lastLocation.getBearing():0,lastLocation.hasAccuracy()?lastLocation.getAccuracy():0,lost,null);}
     private String snapshotSignature(ApiClient.Snapshot s){if(s==null)return "";int r=0,o=0;for(ApiClient.Participant x:s.participants){if(x.ready)r++;if(x.online)o++;}long e=s.events.isEmpty()?0:s.events.get(s.events.size()-1).id;return s.status+":"+s.participants.size()+":"+r+":"+o+":"+s.events.size()+":"+e+":"+s.routeRevision;}
     private void analyzeSnapshot(){maxGapMeters=0;splitAfterOrder=0;offRouteCount=0;if(snapshot==null)return;ArrayList<ApiClient.Participant> a=new ArrayList<>();for(ApiClient.Participant x:snapshot.participants)if(x.online&&x.hasLocation)a.add(x);Collections.sort(a,(x,y)->Integer.compare(x.order,y.order));for(ApiClient.Participant x:a)if(routeModel!=null&&routeModel.usable()){RouteModel.Projection pr=routeModel.project(x.lat,x.lng);if(pr!=null&&pr.offRouteMeters>180)offRouteCount++;if(x.id.equals(participantId)&&pr!=null){myRouteMeters=pr.alongMeters;myOffRouteMeters=pr.offRouteMeters;}}for(int i=1;i<a.size();i++){ApiClient.Participant x=a.get(i-1),y=a.get(i);double d;if(routeModel!=null&&routeModel.usable()){RouteModel.Projection px=routeModel.project(x.lat,x.lng),py=routeModel.project(y.lat,y.lng);d=(px!=null&&py!=null&&px.offRouteMeters<500&&py.offRouteMeters<500)?routeModel.alongGap(px,py):RouteModel.distanceMeters(x.lat,x.lng,y.lat,y.lng);}else d=RouteModel.distanceMeters(x.lat,x.lng,y.lat,y.lng);int di=(int)Math.round(d);if(di>maxGapMeters){maxGapMeters=di;splitAfterOrder=x.order;}}split=maxGapMeters>gapThreshold;}
