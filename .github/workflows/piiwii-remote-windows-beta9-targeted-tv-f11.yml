name: PiiWii Remote Windows beta9 - targeted PiiWii TV F11

on:
  push:
    branches: [piiwii-remote-windows-beta9-targeted-tv-f11]
    paths:
      - '.github/workflows/piiwii-remote-windows-beta9-targeted-tv-f11.yml'
      - 'piiwii-remote-windows-beta9/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  windows:
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Reconstruct beta8 then apply targeted F11 patch
        shell: bash
        run: |
          set -euo pipefail
          BASE="$(cygpath -u "$RUNNER_TEMP")"
          bash piiwii-remote-v2-lot12/reconstruct-lot12.sh "$BASE/piiwii-beta9-src"
          ROOT="$BASE/piiwii-beta9-src/piiwii-remote"
          base64 -d piiwii-remote-v2-windows-beta4/beta4.patch.xz.b64 | xz -d > "$BASE/beta4.patch"
          (cd "$ROOT" && patch -p1 < "$BASE/beta4.patch")
          mkdir -p "$ROOT/windows-agent/installer"
          cp piiwii-remote-v2-windows-beta4/PiiWiiRemoteAgent.iss "$ROOT/windows-agent/installer/"
          base64 -d piiwii-remote-v2-windows-beta5/beta5.patch.xz.b64 | xz -d > "$BASE/beta5.patch"
          (cd "$ROOT" && patch --binary -p2 < "$BASE/beta5.patch")
          base64 -d piiwii-remote-v2-windows-beta6/windows-beta6.patch.xz.b64 | xz -d > "$BASE/beta6.patch"
          (cd "$ROOT" && patch --binary -p1 < "$BASE/beta6.patch")
          python - "$ROOT/windows-agent/src/PiiWiiRemoteAgent.cpp" <<'PY2'
          from pathlib import Path
          import sys
          p=Path(sys.argv[1]); s=p.read_text(encoding='utf-8')
          old='PiiWii Remote Agent\\n2.0.0-beta5\\n'; new='PiiWii Remote Agent\\n2.0.0-beta6\\n'
          if old not in s: raise SystemExit('beta6 baseline not found')
          p.write_text(s.replace(old,new,1),encoding='utf-8')
          PY2
          python - "$ROOT/windows-agent/src/PiiWiiRemoteAgent.cpp" "$ROOT/windows-agent/src/PiiWiiRemoteAgent.rc" "$ROOT/windows-agent/installer/PiiWiiRemoteAgent.iss" <<'PY3'
          from pathlib import Path
          import sys
          for name in sys.argv[1:]:
              p=Path(name); s=p.read_text(encoding='utf-8-sig').replace('\r\n','\n').replace('\r','\n'); p.write_text(s,encoding='utf-8',newline='\n')
          PY3
          (cd "$ROOT" && patch -p1 < "$GITHUB_WORKSPACE/piiwii-remote-v2-windows-beta7/windows-beta7-normalized.patch")
          python - "$ROOT/windows-agent/src/PiiWiiRemoteAgent.cpp" "$ROOT/windows-agent/src/PiiWiiRemoteAgent.rc" "$ROOT/windows-agent/installer/PiiWiiRemoteAgent.iss" <<'PY4'
          from pathlib import Path
          import sys
          cpp, rc, iss = map(Path, sys.argv[1:4])
          s=cpp.read_text(encoding='utf-8')
          s=s.replace('2.0.0-beta7','2.0.0-beta8')
          needle='    const WORD vk = keyboardVirtualKeyForCommand(command);\n'
          block='''    // PiiWii TV : touches virtuelles réservées, sans action sur le volume/navigation Windows.\n    if (command == "PIIWII_TV_CHANNEL_DOWN") return sendVirtualKey(VK_F13);\n    if (command == "PIIWII_TV_CHANNEL_UP") return sendVirtualKey(VK_F14);\n    if (command == "PIIWII_TV_VOLUME_DOWN") return sendVirtualKey(VK_F15);\n    if (command == "PIIWII_TV_VOLUME_UP") return sendVirtualKey(VK_F16);\n    if (command == "PIIWII_TV_MUTE") return sendVirtualKey(VK_F17);\n'''
          if needle not in s: raise SystemExit('beta8 insertion point missing')
          s=s.replace(needle,block+needle,1)
          cpp.write_text(s,encoding='utf-8')
          rc.write_text(rc.read_text(encoding='utf-8').replace('2,0,0,14','2,0,0,15').replace('2.0.0-beta7','2.0.0-beta8'),encoding='utf-8')
          iss.write_text(iss.read_text(encoding='utf-8').replace('2.0.0-beta7','2.0.0-beta8').replace('2.0.0.14','2.0.0.15'),encoding='utf-8')
          PY4
          (cd "$ROOT/windows-agent" && patch -p1 < "$GITHUB_WORKSPACE/piiwii-remote-windows-beta9/targeted-tv-f11.patch")

      - name: Static audit
        shell: pwsh
        run: |
          $root=Join-Path $env:RUNNER_TEMP 'piiwii-beta9-src\piiwii-remote\windows-agent'
          $cpp=Get-Content (Join-Path $root 'src\PiiWiiRemoteAgent.cpp') -Raw -Encoding UTF8
          foreach($needle in @('2.0.0-beta9','PIIWII_TV_BROWSER_F11','findPiiWiiTvBrowserWindow','title.find(L"PiiWii TV")','PIIWII_TV_CHANNEL_DOWN','VK_F13','PIIWII_TV_MUTE','VK_F17','KEYBOARD_KEY','VOICE_START')){ if(-not $cpp.Contains($needle)){throw "Missing: $needle"} }
          $iss=Get-Content (Join-Path $root 'installer\PiiWiiRemoteAgent.iss') -Raw -Encoding UTF8
          if(-not $iss.Contains('2.0.0-beta9') -or -not $iss.Contains('VersionInfoProductVersion=2.0.0.16')){throw 'Installer beta9 version missing'}
          'STATIC BETA9 TARGETED F11 AUDIT: OK'

      - uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Compile Agent beta9
        shell: cmd
        working-directory: ${{ runner.temp }}\piiwii-beta9-src\piiwii-remote\windows-agent
        run: |
          if not exist build mkdir build
          rc /nologo /fo build\PiiWiiRemoteAgent.res src\PiiWiiRemoteAgent.rc
          cl /nologo /utf-8 /std:c++17 /EHsc /W4 /DUNICODE /D_UNICODE /DWIN32 /D_WINDOWS /O2 src\PiiWiiRemoteAgent.cpp build\PiiWiiRemoteAgent.res gdi32.lib user32.lib advapi32.lib PowrProf.lib bcrypt.lib shell32.lib winmm.lib /link /SUBSYSTEM:WINDOWS /OUT:build\PiiWii-Remote-Agent-2.0.0-beta9.exe

      - name: Validate Agent beta9
        shell: pwsh
        run: |
          $root=Join-Path $env:RUNNER_TEMP 'piiwii-beta9-src\piiwii-remote\windows-agent'
          $exe=Join-Path $root 'build\PiiWii-Remote-Agent-2.0.0-beta9.exe'
          $p=Start-Process -FilePath $exe -ArgumentList '--self-test' -Wait -PassThru
          if($p.ExitCode -ne 0){throw "Agent self-test failed: $($p.ExitCode)"}
          $r=Start-Process -FilePath $exe -ArgumentList '--reopen-self-test' -Wait -PassThru
          if($r.ExitCode -ne 0){throw "Reopen self-test failed: $($r.ExitCode)"}
          if((Get-Item $exe).VersionInfo.ProductVersion -notlike '2.0.0-beta9*'){throw 'Bad beta9 ProductVersion'}

      - name: Build beta9 installer
        shell: pwsh
        run: |
          $root=Join-Path $env:RUNNER_TEMP 'piiwii-beta9-src\piiwii-remote\windows-agent'
          $iscc='C:\Program Files (x86)\Inno Setup 6\ISCC.exe'
          if(-not(Test-Path $iscc)){ choco install innosetup --no-progress -y }
          & $iscc (Join-Path $root 'installer\PiiWiiRemoteAgent.iss')
          if($LASTEXITCODE -ne 0){throw "Inno Setup failed: $LASTEXITCODE"}
          $setup=Join-Path $root 'installer\output\PiiWii-Remote-Setup-2.0.0-beta9.exe'
          if(-not(Test-Path $setup)){throw 'Setup beta9 missing'}

      - name: Package beta9
        shell: pwsh
        run: |
          $root=Join-Path $env:RUNNER_TEMP 'piiwii-beta9-src\piiwii-remote\windows-agent'
          $out=Join-Path $env:RUNNER_TEMP 'piiwii-beta9-out'; New-Item -ItemType Directory -Force $out|Out-Null
          Copy-Item (Join-Path $root 'build\PiiWii-Remote-Agent-2.0.0-beta9.exe') $out
          Copy-Item (Join-Path $root 'installer\output\PiiWii-Remote-Setup-2.0.0-beta9.exe') $out
          Compress-Archive -Path (Join-Path $root 'src'),(Join-Path $root 'installer') -DestinationPath (Join-Path $out 'PiiWii-Remote-Windows-2.0.0-beta9-Sources.zip') -Force
          @('PIIWII REMOTE WINDOWS 2.0.0-beta9','Dedicated PIIWII_TV_BROWSER_F11 targets a visible window whose title contains PiiWii TV','If no PiiWii TV window is found, no F11 is sent','Generic KEYBOARD_KEY F11 remains unchanged','F13-F17 PiiWii TV CH/VOL/MUTE preserved','Agent self-test + reopen self-test: OK') | Set-Content -Encoding UTF8 (Join-Path $out 'PiiWii-Remote-Windows-2.0.0-beta9-AUDIT.txt')
          Get-ChildItem $out -File | ForEach-Object {"$((Get-FileHash -Algorithm SHA256 $_.FullName).Hash.ToLower())  $($_.Name)"} | Set-Content -Encoding ascii (Join-Path $out 'PiiWii-Remote-Windows-2.0.0-beta9-SHA256.txt')

      - uses: actions/upload-artifact@v4
        with:
          name: PiiWii-Remote-Windows-2.0.0-beta9-Targeted-TV-F11
          path: ${{ runner.temp }}\piiwii-beta9-out\*
          retention-days: 7
          if-no-files-found: error
