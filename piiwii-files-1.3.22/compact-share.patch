diff -ruN '--exclude=PiiWii-Files-1.3.14-ICON-MASTER.png' a/app/build.gradle b/app/build.gradle
--- a/app/build.gradle	2026-09-07 23:57:30.016643758 +0000
+++ b/app/build.gradle	2026-09-08 00:05:04.967795394 +0000
@@ -10,8 +10,8 @@
         applicationId 'ch.piiwii.files'
         minSdk 26
         targetSdk 29
-        versionCode 25
-        versionName '1.3.21'
+        versionCode 26
+        versionName '1.3.22'
     }
 
     buildTypes {
diff -ruN '--exclude=PiiWii-Files-1.3.14-ICON-MASTER.png' a/app/src/main/java/ch/piiwii/files/MainActivity.java b/app/src/main/java/ch/piiwii/files/MainActivity.java
--- a/app/src/main/java/ch/piiwii/files/MainActivity.java	2026-09-07 23:58:37.008160152 +0000
+++ b/app/src/main/java/ch/piiwii/files/MainActivity.java	2026-09-08 00:05:04.975143475 +0000
@@ -1603,9 +1603,6 @@
         titleRow.addView(close, new LinearLayout.LayoutParams(dp(42), dp(42)));
         card.addView(titleRow);
 
-        Space filler = new Space(this);
-        card.addView(filler, new LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, 0, 1f));
-
         ScrollView otherScroll = new ScrollView(this);
         otherScroll.setFillViewport(true);
         otherScroll.setVisibility(View.GONE);
@@ -1653,8 +1650,16 @@
         otherToggle.setOnClickListener(v -> {
             boolean open = otherScroll.getVisibility() == View.VISIBLE;
             otherScroll.setVisibility(open ? View.GONE : View.VISIBLE);
-            filler.setVisibility(open ? View.VISIBLE : View.GONE);
             arrow.setText(open ? "⌃" : "⌄");
+            Window window = dialog.getWindow();
+            if (window != null) {
+                android.view.WindowManager.LayoutParams params = new android.view.WindowManager.LayoutParams();
+                params.copyFrom(window.getAttributes());
+                params.height = open
+                        ? ViewGroup.LayoutParams.WRAP_CONTENT
+                        : Math.min(getResources().getDisplayMetrics().heightPixels - dp(28), dp(620));
+                window.setAttributes(params);
+            }
         });
         LinearLayout.LayoutParams otp = new LinearLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, ViewGroup.LayoutParams.WRAP_CONTENT);
         otp.setMargins(0, dp(8), 0, 0);
@@ -1702,7 +1707,7 @@
             android.view.WindowManager.LayoutParams lp = new android.view.WindowManager.LayoutParams();
             lp.copyFrom(w.getAttributes());
             lp.width = Math.min(getResources().getDisplayMetrics().widthPixels - dp(18), dp(560));
-            lp.height = Math.min(getResources().getDisplayMetrics().heightPixels - dp(28), dp(620));
+            lp.height = ViewGroup.LayoutParams.WRAP_CONTENT;
             lp.gravity = Gravity.BOTTOM | Gravity.CENTER_HORIZONTAL;
             lp.y = dp(8);
             w.setAttributes(lp);
