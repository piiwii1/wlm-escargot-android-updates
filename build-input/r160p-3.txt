+            p.setColor(Color.rgb(45,164,255));
+            canvas.drawRect(b, p);
+
+            float w=b.width(), h=b.height();
+            // Clouds
+            p.setColor(Color.argb(215,255,255,255));
+            drawCloud(canvas, w*0.16f, h*0.12f, w*0.07f);
+            drawCloud(canvas, w*0.78f, h*0.18f, w*0.06f);
+            // Rounded green hills
+            p.setColor(Color.rgb(68,190,88));
+            canvas.drawCircle(w*0.18f,h*0.92f,w*0.26f,p);
+            p.setColor(Color.rgb(46,168,75));
+            canvas.drawCircle(w*0.77f,h*0.94f,w*0.31f,p);
+            p.setColor(Color.rgb(112,211,99));
+            canvas.drawCircle(w*0.52f,h*0.98f,w*0.20f,p);
+        }
+        private void drawCloud(Canvas c,float cx,float cy,float r){
+            c.drawCircle(cx-r*0.55f,cy,r*0.55f,p);
+            c.drawCircle(cx,cy-r*0.22f,r*0.72f,p);
+            c.drawCircle(cx+r*0.62f,cy,r*0.50f,p);
+            c.drawRoundRect(cx-r*1.15f,cy-r*0.15f,cx+r*1.15f,cy+r*0.55f,r*0.35f,r*0.35f,p);
+        }
+        @Override public void setAlpha(int alpha){p.setAlpha(alpha);}
+        @Override public void setColorFilter(android.graphics.ColorFilter cf){p.setColorFilter(cf);}
+        @Override public int getOpacity(){return PixelFormat.TRANSLUCENT;}
+    }
+
+    private static class MarioBrickDrawable extends Drawable {
+        private final Paint p = new Paint(Paint.ANTI_ALIAS_FLAG);
+        @Override public void draw(Canvas canvas){
+            android.graphics.Rect b=getBounds();
+            p.setStyle(Paint.Style.FILL);
+            p.setColor(Color.rgb(165,71,28));
+            canvas.drawRect(b,p);
+            float row=Math.max(10f,b.height()/3f);
+            p.setColor(Color.rgb(116,45,18));
+            p.setStrokeWidth(Math.max(2f,b.height()/30f));
+            p.setStyle(Paint.Style.STROKE);
+            for(float y=b.top+row;y<b.bottom;y+=row) canvas.drawLine(b.left,y,b.right,y,p);
+            p.setStyle(Paint.Style.FILL);
+            float brickW=Math.max(28f,b.width()/5f);
+            p.setColor(Color.rgb(213,98,40));
+            for(int r=0;r<3;r++){
+                float y=b.top+r*row; float off=(r%2==0)?0:brickW/2f;
+                for(float x=b.left-off;x<b.right;x+=brickW){
+                    android.graphics.RectF rf=new android.graphics.RectF(x+2,y+2,Math.min(x+brickW-2,b.right-1),Math.min(y+row-2,b.bottom-1));
+                    canvas.drawRoundRect(rf,4,4,p);
+                }
+            }
+        }
+        @Override public void setAlpha(int alpha){p.setAlpha(alpha);}
+        @Override public void setColorFilter(android.graphics.ColorFilter cf){p.setColorFilter(cf);}
+        @Override public int getOpacity(){return PixelFormat.OPAQUE;}
+    }
+
+    private static class MinionBackgroundDrawable extends Drawable {
+        private final Paint p=new Paint(Paint.ANTI_ALIAS_FLAG);
+        @Override public void draw(Canvas canvas){
+            android.graphics.Rect b=getBounds();
+            p.setStyle(Paint.Style.FILL);
+            p.setColor(Color.rgb(255,219,38));
+            canvas.drawRect(b,p);
+            float w=b.width(), h=b.height();
+            // soft denim-blue band and abstract goggle circles
+            p.setColor(Color.argb(42,24,61,104));
+            canvas.drawRect(b.left,h*0.76f,b.right,h,p);
+            p.setStyle(Paint.Style.STROKE);
+            p.setStrokeWidth(Math.max(3f,w/120f));
+            p.setColor(Color.argb(70,67,78,92));
+            canvas.drawCircle(w*0.14f,h*0.12f,w*0.055f,p);
+            canvas.drawCircle(w*0.86f,h*0.12f,w*0.055f,p);
+            p.setStyle(Paint.Style.FILL);
+            p.setColor(Color.argb(32,255,255,255));
+            canvas.drawCircle(w*0.14f,h*0.12f,w*0.035f,p);
+            canvas.drawCircle(w*0.86f,h*0.12f,w*0.035f,p);
+        }
+        @Override public void setAlpha(int alpha){p.setAlpha(alpha);}
+        @Override public void setColorFilter(android.graphics.ColorFilter cf){p.setColorFilter(cf);}
+        @Override public int getOpacity(){return PixelFormat.TRANSLUCENT;}
+    }
+
     private static class StarburstDrawable extends Drawable {
         private final Paint fill=new Paint(Paint.ANTI_ALIAS_FLAG);
         private final Paint stroke=new Paint(Paint.ANTI_ALIAS_FLAG);
diff -ruN '--exclude=README.md' '--exclude=BUILD-INFO.txt' base152/app/src/main/res/layout/activity_main.xml rabais160/app/src/main/res/layout/activity_main.xml
--- base152/app/src/main/res/layout/activity_main.xml	2026-09-15 23:18:58.000000000 +0000
+++ rabais160/app/src/main/res/layout/activity_main.xml	2026-09-15 23:55:06.036281878 +0000
@@ -446,6 +446,8 @@
                 <Button android:id="@+id/themeXP" style="@style/ThemeChoice" android:layout_marginTop="8dp" android:text="Windows XP  ·  Classique" />
                 <Button android:id="@+id/themeMSN" style="@style/ThemeChoice" android:layout_marginTop="8dp" android:text="MSN Messenger  ·  Bleu clair" />
                 <Button android:id="@+id/themeDOS" style="@style/ThemeChoice" android:layout_marginTop="8dp" android:text="MS-DOS  ·  Terminal vert" />
+                <Button android:id="@+id/themeMario" style="@style/ThemeChoice" android:layout_marginTop="8dp" android:text="Super Mario  ·  Jeu de plateforme" />
+                <Button android:id="@+id/themeMinion" style="@style/ThemeChoice" android:layout_marginTop="8dp" android:text="Minion  ·  Jaune &amp; bleu" />
             </LinearLayout>
 
